---
import Layout from '@/layouts/Layout.astro';

// Zabezpieczenie strony - wymaga zalogowania
const user = Astro.locals.user;

if (!user) {
  // Redirect do logowania z redirectTo param
  const redirectTo = encodeURIComponent(Astro.url.pathname);
  return Astro.redirect(`/auth/login?redirectTo=${redirectTo}`);
}

// Opcjonalnie: sprawdzenie roli (jeśli tylko dla adopters)
if (user.role !== 'adopter' && user.role !== 'admin') {
  return Astro.redirect('/403'); // lub inna strona błędu
}
---

<Layout title="Generuj rekomendacje - AdoptMe">
  <div class="container mx-auto px-4 py-12">
    <div class="max-w-2xl mx-auto">
      <h1 class="text-3xl font-bold mb-4">Generuj rekomendacje AI</h1>

      <div class="mb-6 p-4 rounded-md bg-green-50 dark:bg-green-900/20 text-green-900 dark:text-green-100">
        <p class="text-sm">
          Zalogowany jako: <strong>{user.email}</strong> (Rola: {user.role})
        </p>
      </div>

      <div class="space-y-6">
        <p class="text-muted-foreground">
          Ta strona jest chroniona i wymaga zalogowania.
          Tutaj będzie moduł rekomendacji AI psów na podstawie ankiety.
        </p>

        <div class="p-6 border rounded-lg">
          <h2 class="text-xl font-semibold mb-4">Funkcjonalność w przygotowaniu</h2>
          <p class="text-sm text-muted-foreground">
            Moduł rekomendacji AI będzie dostępny w kolejnej fazie projektu.
          </p>
        </div>

        <div class="flex gap-3">
          <a
            href="/dogs"
            class="inline-flex items-center justify-center rounded-md text-sm font-medium transition-all h-9 px-4 py-2 bg-primary text-primary-foreground hover:bg-primary/90"
          >
            Przeglądaj psy
          </a>
          <a
            href="/"
            class="inline-flex items-center justify-center rounded-md text-sm font-medium transition-all h-9 px-4 py-2 border bg-background shadow-xs hover:bg-accent hover:text-accent-foreground"
          >
            Strona główna
          </a>
        </div>
      </div>
    </div>
  </div>
</Layout>

