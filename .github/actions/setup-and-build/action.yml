name: Setup and Build
description: Setup Node, install dependencies, and run linter

runs:
  using: composite
  steps:
    - name: 🟦 Setup Node
      uses: actions/setup-node@v6
      with:
        node-version-file: '.nvmrc'
        cache: 'npm'

    - name: 📦 Install dependencies
      run: npm ci
      shell: bash
